name: Hyperopt Runner (WFO) — placeholder, not wired in until a later pass

# This is the single SHARED workflow that every WFO bot's hyperopt /
# one-shot runs will dispatch into. Per the planning prompt, WFO's
# own web frontend and job-shape are a separate, later planning
# track. This file exists now only so the filename and trigger shape
# are reserved and won't need renaming later — it does no real work
# yet.
#
# Restored as of Pass 28 (this file was missing from
# .github/workflows/ entirely — see Fix 4 in the corresponding
# session notes). Content is unchanged from Pass 1: as of Pass 28,
# nothing in the codebase references `run-hyperopt` or dispatches
# into this workflow yet, so it remains a genuine reserved
# placeholder rather than something that needs updating to match
# newer functionality.

on:
  workflow_dispatch:
  repository_dispatch:
    types: [run-hyperopt]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Not yet implemented
        run: |
          echo "hyperopt-runner.yml is a reserved placeholder as of Pass 1, still unwired as of Pass 28."
          echo "Real hyperopt/WFO execution logic lands in a later pass."
